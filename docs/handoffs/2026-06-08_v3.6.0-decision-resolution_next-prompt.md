# Next session — Implement agent-review-panel v3.6.0 (Human-in-the-Loop Decision Resolution)

Paste-ready cold-start prompt. Everything you need is here or in the linked spec.

## Start here
```
cd ~/Documents/agent-review-panel-grill-spec      # worktree, branch feat/grill-me-decision-resolution (off main @ v3.5.0)
git status        # should be clean; you continue on this branch
```
Read the approved design spec FIRST — it is the source of truth:
`docs/superpowers/specs/2026-06-08-grill-me-decision-resolution-design.md`

## Context (what's already decided — do NOT re-brainstorm)
The design is **approved**. v3.6.0 adds a human-in-the-loop layer: one **grill
engine** (grill-me pattern inlined — NOT a hard dep on the external grill-me
skill) with three entry points — ① upstream Phase 0 (plan-mode decision lock),
② downstream terminal Phase 17 (resolve judge `[USER-DECISION]`), ③ HTML feedback
round-trip (annotate findings → export self-contained bundle → paste back →
questions route to grill). Plus the judge `[USER-DECISION]` label + Phase 14.5
guard, and a plan-review-integrator handoff. **last30days `[COMMUNITY-SIGNAL]` is
v3.7.0 — out of scope here.**

## First action
Invoke **`writing-plans`** to turn spec §12's 7 build stages into a detailed
implementation plan, then execute with **TDD** (your test-driven-development skill).

## Build order (spec §12) — each stage independently testable
1. Judge `[USER-DECISION]` label (`references/prompt-templates.md` Phase 14) + Phase 14.5 guard + **adversarial fixture** (a verifiable claim the judge tries to defer — must be kicked back).
2. Grill engine core (Phase 17 prompt, defined ONCE) + terminal downstream + `state/phase_17_decision_resolution.md`.
3. Upstream Phase 0 (plan-mode gate) + "Locked Decisions" Context-Brief block + reviewer-prompt injection.
4. HTML per-card feedback widgets (✅/❓/✏️ + free text) + `localStorage` keyed `{run-id}:{issue-id}`.
5. Export serializer + bundle grammar (spec §6) + **round-trip test** (annotations → bundle string → parsed structure).
6. Ingest parser (parse from paste alone) + status routing (signoff→close, change→action, question→grill).
7. plan-review-integrator handoff (plan-mode, user-gated).

## Guardrails (from the spec + repo conventions)
- **§3.2 guard is load-bearing** — without it `[USER-DECISION]` becomes a correctness-dodge. Test it adversarially.
- **Golden byte-stability**: make the Phase 15.1 `[USER-DECISION]` report section **conditional** (renders only when ≥1 item) so existing golden fixtures stay byte-identical; add ONE new fixture with a `[USER-DECISION]` item.
- **HTML JS must not collide** with existing keyboard-nav / filter / deep-link / Chart.js logic — assert in tests.
- **Version bump = ALL files** (spec §14): `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `package.json`, `README.md`, `SKILL.md` header, `CHANGELOG.md`, `references/changelog.md`. Add v3.7.0 last30days item to `ROADMAP.md`.
- **Run the full test suite** (`tests/*.test.mjs`) before PR; confirm all goldens green.
- **Git hygiene**: explicit `git add <file>`, never `-A`. Do NOT touch the `docs/agency-agents-evaluation` branch's WIP.

## Edit surface (verified tracked paths — spec §11)
- `skills/agent-review-panel/references/prompt-templates.md` (judge label, guard, Phase 17 prompt, reviewer block, HTML spec)
- `skills/agent-review-panel/SKILL.md` (Phase 0, 14/14.5 docs, conditional 15.1, 15.3 features, Phase 17 + ingest, trigger desc, version header)
- version files + `CHANGELOG.md` + `references/changelog.md` + `ROADMAP.md`
- `tests/` (new `[USER-DECISION]` fixture, bundle round-trip test, HTML assertions, plan-mode-offers-upstream-grill trigger test)

## Definition of done
All 7 stages built + tested; v3.6.0 across all version files; full suite green (goldens byte-stable + new tests); PR opened from `feat/grill-me-decision-resolution` with a test-plan checklist. Then run a self-review (`requesting-code-review` or the panel itself — dogfood it) before merge.
