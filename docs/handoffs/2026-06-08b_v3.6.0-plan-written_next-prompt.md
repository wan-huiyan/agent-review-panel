# Next session — Build agent-review-panel v3.6.0 from the written plan

Paste-ready cold-start prompt. The implementation plan already exists and is the source of truth — do NOT re-plan or re-brainstorm.

## Start here
```
cd ~/Documents/agent-review-panel-grill-spec      # worktree, branch feat/grill-me-decision-resolution (off main @ v3.5.0)
git status        # should be clean, in sync with origin @ 35564ba
git log --oneline -3
```

Read FIRST (in order):
1. `docs/superpowers/plans/2026-06-08-grill-me-decision-resolution.md` — **the plan; the source of truth.** 9 TDD tasks with exact failing-test code + exact markdown content blocks.
2. `docs/superpowers/specs/2026-06-08-grill-me-decision-resolution-design.md` — the approved design (section refs §N appear throughout the plan).

## What this is
v3.6.0 = a human-in-the-loop decision-resolution layer: one **grill engine** (grill-me pattern inlined, NOT a hard dep) with three entry points (① upstream Phase 0 plan-mode lock, ② terminal `[USER-DECISION]` resolution, ③ HTML feedback bundle ingest), a judge `[USER-DECISION]` label guarded by a Phase 14.5 kick-back, HTML per-card feedback + a self-contained export bundle, and a plan-review-integrator handoff. The plan turns spec §12's 7 build stages into 9 tasks.

## How to execute
**Inline + TDD.** Work Tasks 1→9 in order. Each task: write the failing `.mjs` assertion → run it red → edit the markdown spec → run it green → commit. Do **not** use subagent-per-task: the bundle grammar must stay byte-identical across producer/example/parser/test, and the Phase 17 grill prompt is "defined once" — both are fragile to subagent drift.

This is a **skill-spec repo**: "code" is markdown (`SKILL.md` + `references/prompt-templates.md`); "tests" are `node:test` `.mjs` files asserting on that markdown content + sample fixtures. The v3.5.0 `[NO-DEBATE]` feature (`tests/behavioral-assertions.test.mjs` → `describe("v3.5.0 …")` + a new fixture + a `report-structure.test.mjs` parser extension) is the exact precedent to mirror.

## Guardrails (verified this session — don't re-derive)
- **`scripts/release-check.sh` is the real version gate, not `npm test`.** It enforces `eval-suite.json` version, SKILL.md H1 + HTML-footer instruction, a ROADMAP **v3.6.0 row**, a CHANGELOG `[3.6.0]` section, and **README test-count == actual** (adding tests forces a README count update). Run it on `main` first to baseline, then as the Task 8 gate.
- **Register new test files** (`tests/bundle-roundtrip.test.mjs`) in `package.json` `scripts.test` or `node --test` skips them.
- **Golden byte-stability:** the `[USER-DECISION]` deferral is a conditional `###` subsection (invisible to the `## `-only golden fingerprint) + ONE new complete fixture. `[USER-DECISION]` never appears as a numbered action item.
- **The §3.2 guard** ships as a worked-example few-shot in the Phase 14.5 prompt + presence tests — verified as *specified*, not *fired*. Say so in the PR.
- **HTML rendering instructions** live in `prompt-templates.md` Phase 15.3, not SKILL.md.
- **Git hygiene:** explicit `git add <file>`, never `-A`. Don't touch the `docs/agency-agents-evaluation` branch.
- **Do NOT close #58** (v3.7.0 last30days deferral, out of scope).

## Baseline (confirm before starting)
- `npm test` → 443 tests, 0 fail.
- `bash scripts/release-check.sh` → all pass, canonical version `3.5.0`.

## Definition of done
All 9 tasks built + tested; v3.6.0 across all version files; full suite green (goldens byte-stable + new tests); `release-check.sh` all-pass with canonical `3.6.0`; PR opened from `feat/grill-me-decision-resolution` with a test-plan checklist + the guard caveat. Then self-review (dogfood the panel or `requesting-code-review`) before merge; tag `v3.6.0` + `gh release create` per repo convention.
