{
  "name": "agent-review-panel",
  "version": "2.9.0",
  "private": true,
  "description": "Multi-agent adversarial review panel for Claude Code",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "test:triggers": "node --test tests/trigger-classification.test.mjs",
    "test:manifest": "node --test tests/manifest-consistency.test.mjs",
    "test:eval-suite": "node --test tests/eval-suite-integrity.test.mjs"
  },
  "type": "module"
}
